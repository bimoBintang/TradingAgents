typing-extensions
langchain-core
langchain-openai
langchain-experimental
pandas
yfinance
stockstats
langgraph
rank-bm25
setuptools
backtrader
parsel
requests
tqdm
pytz
redis
rich
typer
questionary
langchain_anthropic
langchain-google-genai
pydantic
ccxt           
alpaca-py      

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
pydantic>=2.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
slowapi>=0.1.9
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.0
celery[redis]>=5.3.0
prometheus-client>=0.20.0
prometheus-fastapi-instrumentator>=6.0.0
# Used by api/services/pattern_detector.py — was missing before, had to be
# installed manually when first running the backend (2026-08-25).
scipy>=1.11.0
scikit-learn>=1.3.0

# ==================== MCP Server ====================
# TradingAgents-as-MCP-server (mcp_server/server.py) — exposes the
# LangChain @tool functions in tradingagents/agents/utils/ as MCP tools.
mcp[cli]>=1.2.0

# ==================== Tests ====================
# Not previously declared anywhere despite tests/ + pytest.ini existing —
# was apparently always installed ad hoc. Declaring it now so `pip
# install -r requirements.txt` alone is enough to run the test suite.
pytest>=8.0.0
pytest-asyncio>=0.24.0

# ==================== Runtime deps missing from this file ====================
# All four below were imported by api/main.py's router chain (directly or via
# orchestrator/mcp/tradingview_mcp_client.py) but not declared here — a fresh
# `pip install -r requirements.txt` + `uvicorn api.main:app` crashed on each
# in turn at import time until installed manually (2026-08-27).
Pillow>=10.0.0                # orchestrator/mcp/tradingview_mcp_client.py — screenshot handling
tradingview-ta>=3.3.0         # tradingagents/dataflows/tradingview.py — TA_Handler/Interval
APScheduler>=3.10.0           # tradingagents/scheduler/scheduler.py
sqlite-vec>=0.1.9             # vector memory storage backend
playwright>=1.62.0            # orchestrator/mcp/tradingview_mcp_client.py — CDP automation (connect_over_cdp only, no browser download needed)
